<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicalendar.mapper.ScheduleMapper">

    <select id="selectByDate" resultType="com.aicalendar.entity.Schedule">
        SELECT * FROM schedule 
        WHERE start_time >= #{startOfDay} AND start_time &lt; #{endOfDay} 
        ORDER BY start_time ASC
    </select>

    <select id="selectByType" resultType="com.aicalendar.entity.Schedule">
        SELECT * FROM schedule 
        WHERE type = #{type} 
        ORDER BY start_time ASC
    </select>

    <select id="searchByKeyword" resultType="com.aicalendar.entity.Schedule">
        SELECT * FROM schedule 
        WHERE title LIKE CONCAT('%', #{keyword}, '%') 
        ORDER BY start_time ASC
    </select>

    <select id="selectByTimeRange" resultType="com.aicalendar.entity.Schedule">
        SELECT * FROM schedule 
        WHERE start_time >= #{startTime} AND end_time &lt;= #{endTime} 
        ORDER BY start_time ASC
    </select>

</mapper>